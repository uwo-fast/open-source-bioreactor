# Seams the renderer leaves that this project cannot close, measured, not written by hand.
# One line per part that does not close: <build> <part> <over> <dup>, where over is edges
# shared by more than two faces and dup is triangles that repeat. Both are zero-volume:
# the surface is still closed, and a slicer drops them.
#
# Rewrite with `just check-seams-update`, and read the diff before keeping it.
#
# All of these are one shape of defect - two solids meeting exactly face to face, so the
# renderer keeps the shared face instead of merging it. The lids' come from the pinned
# bayonet-lock library, which is fetched rather than vendored, so that fix is upstream;
# the probes' come from the transition meeting the collet. TODO.md carries both.
jar_10L_220x305 lid 6 3
jar_10L_magnetic lid 6 3
jar_1gal_180x197 port_do_probe 1 1
jar_1p5L_magnetic lid 2 1
jar_1p5L_magnetic port_do_probe 2 2
jar_6p5gal_305x470 port_do_probe 2 2
